{
  "name": "whatsapp-tizen-tv-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local Samsung/Tizen TV simulator POC for sending WhatsApp templates through Meta Cloud API.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.2.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
